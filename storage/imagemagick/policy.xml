<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE policymap [
  <!ELEMENT policymap (policy)*>
  <!ATTLIST policymap xmlns CDATA #FIXED ''>
  <!ELEMENT policy EMPTY>
  <!ATTLIST policy xmlns CDATA #FIXED '' domain NMTOKEN #REQUIRED
    name NMTOKEN #IMPLIED pattern CDATA #IMPLIED rights NMTOKEN #IMPLIED
    stealth NMTOKEN #IMPLIED value CDATA #IMPLIED>
]>
<!--
  CutContour hardened ImageMagick policy.

  Blocks the coders and delegates that historically enable SSRF, arbitrary file
  read, and RCE when processing user-supplied images (ImageTragick class):
    - MSL / MVG / LABEL — script/text expansion coders
    - HTTPS / HTTP / URL / FTP — SSRF sinks (metadata endpoints, SSRF-to-file)
    - EPHEMERAL — arbitrary file read via delete-after-open
    - PS / PS2 / PS3 / EPS / PDF — PostScript delegates (via Ghostscript)
    - XPS — XPS delegate

  PDF/PS files still need to be processed for print-shop uploads, but only
  through PdfService (Ghostscript with -dSAFER), never through ImageMagick's
  delegate chain.

  Loaded at process start via MAGICK_CONFIGURE_PATH — see AppServiceProvider::boot.
-->
<policymap>
  <policy domain="delegate" rights="none" pattern="HTTPS" />
  <policy domain="delegate" rights="none" pattern="HTTP" />
  <policy domain="coder" rights="none" pattern="EPHEMERAL" />
  <policy domain="coder" rights="none" pattern="URL" />
  <policy domain="coder" rights="none" pattern="HTTPS" />
  <policy domain="coder" rights="none" pattern="HTTP" />
  <policy domain="coder" rights="none" pattern="FTP" />
  <policy domain="coder" rights="none" pattern="MVG" />
  <policy domain="coder" rights="none" pattern="MSL" />
  <policy domain="coder" rights="none" pattern="TEXT" />
  <policy domain="coder" rights="none" pattern="SHOW" />
  <policy domain="coder" rights="none" pattern="WIN" />
  <policy domain="coder" rights="none" pattern="PLT" />
  <policy domain="coder" rights="none" pattern="PS" />
  <policy domain="coder" rights="none" pattern="PS2" />
  <policy domain="coder" rights="none" pattern="PS3" />
  <policy domain="coder" rights="none" pattern="EPS" />
  <policy domain="coder" rights="none" pattern="PDF" />
  <policy domain="coder" rights="none" pattern="XPS" />
  <policy domain="path" rights="none" pattern="@*" />
  <policy domain="resource" name="memory" value="512MiB" />
  <policy domain="resource" name="map" value="1GiB" />
  <policy domain="resource" name="width" value="16KP" />
  <policy domain="resource" name="height" value="16KP" />
  <policy domain="resource" name="area" value="128MP" />
  <policy domain="resource" name="disk" value="4GiB" />
  <policy domain="resource" name="time" value="120" />
</policymap>
